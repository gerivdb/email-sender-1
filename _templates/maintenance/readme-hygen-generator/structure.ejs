<% /* Section structure du dossier */ %>
## Structure

<% if (typeof files !== 'undefined' && files.length > 0) { %>
```
<%= files.map(f => f.isDir ? f.name + '/' : f.name).join('\n') %>
```
<% } else { %>
Ce dossier ne contient actuellement aucun fichier détecté.
<% } %>
