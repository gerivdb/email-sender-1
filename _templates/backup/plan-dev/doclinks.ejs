<% /*
  Générateur de liens vers la documentation avec descriptions
  Utilisation :
  - Inclure ce fichier dans plan-dev.ejs avec la syntaxe EJS :
    <%~ include('doclinks.ejs', { docLinks: docLinks }) %>
  - 'docLinks' doit être un tableau d'objets { label: 'Titre', url: 'lien', description: 'Description' }
*/ %>
<% if (typeof docLinks !== 'undefined' && docLinks.length > 0) { %>
## Documentation associée
<% docLinks.forEach(function(link) { %>
- [<%= link.label %>](<%= link.url %>) : <%= link.description %>
<% }) %>
<% } %>
