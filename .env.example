# 🔧 Smart Infrastructure Orchestrator Configuration
# Copy this file to .env and customize as needed

# 🌍 Environment Configuration
ENVIRONMENT=development
DEPLOYMENT_PROFILE=development
LOG_LEVEL=info

# 🔢 Service Versions
QDRANT_VERSION=v1.7.0
PROMETHEUS_VERSION=v2.45.0
GRAFANA_VERSION=10.0.0
REDIS_VERSION=7.2-alpine
NODE_EXPORTER_VERSION=v1.6.0
PROMTAIL_VERSION=2.9.0

# 🌐 Port Configuration
RAG_HTTP_PORT=8080
RAG_METRICS_PORT=9090
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
PROMETHEUS_PORT=9091
GRAFANA_PORT=3000
REDIS_PORT=6379
REDIS_COMMANDER_PORT=8081
NODE_EXPORTER_PORT=9100

# 🔧 RAG Server Configuration
RAG_MAX_WORKERS=4
RAG_BATCH_SIZE=100

# 🗄️ QDrant Configuration
QDRANT_LOG_LEVEL=INFO
QDRANT_DELETED_THRESHOLD=0.2
QDRANT_VACUUM_MIN=1000
QDRANT_MAX_REQUEST_SIZE=32

# 📊 Prometheus Configuration
PROMETHEUS_RETENTION=200h
PROMETHEUS_MAX_BLOCK=2h

# 📈 Grafana Configuration
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin123

# 🔄 Redis Configuration
REDIS_MAX_MEMORY=512mb

# 🌐 Network Configuration
NETWORK_SUBNET=172.20.0.0/16

# 🔑 API Keys (set these in production)
NOTION_API_KEY=your_notion_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# 🏗️ Build Configuration
VERSION=dev
BUILD_DATE=

# 📊 Profiles Quick Reference:
# development: Basic development stack (rag-server, qdrant, redis, prometheus, grafana, redis-commander)
# staging: Staging environment with monitoring
# production: Production with full monitoring and logging
# full-stack: Everything including development tools
# monitoring: Only monitoring stack (prometheus, grafana, node-exporter)
# vectorization: Only vector database stack (qdrant)
# cache: Only caching stack (redis)
# logging: Log management stack (promtail)

# 🚀 Quick Start Commands:
# docker-compose --profile development up -d
# docker-compose --profile production up -d
# docker-compose --profile monitoring up -d
