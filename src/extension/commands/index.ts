// Registre des commandes de l'extension
import * as vscode from 'vscode';

export function registerCommands(context: vscode.ExtensionContext) {
  // À compléter : enregistrer les commandes ici
}
