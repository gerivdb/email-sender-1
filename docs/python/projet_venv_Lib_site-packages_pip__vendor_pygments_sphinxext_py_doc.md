Help on module sphinxext:

NAME
    sphinxext

DESCRIPTION
    pygments.sphinxext
    ~~~~~~~~~~~~~~~~~~

    Sphinx extension to generate automatic documentation of lexers,
    formatters and filters.

    :copyright: Copyright 2006-2024 by the Pygments team, see AUTHORS.
    :license: BSD, see LICENSE for details.

CLASSES
    docutils.parsers.rst.Directive(builtins.object)
        PygmentsDoc

    class PygmentsDoc(docutils.parsers.rst.Directive)
     |  PygmentsDoc(name, arguments, options, content, lineno, content_offset, block_text, state, state_machine)
     |
     |  A directive to collect all lexers/formatters/filters and generate
     |  autoclass directives for them.
     |
     |  Method resolution order:
     |      PygmentsDoc
     |      docutils.parsers.rst.Directive
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  document_filters(self)
     |
     |  document_formatters(self)
     |
     |  document_lexers(self)
     |
     |  document_lexers_overview(self)
     |      Generate a tabular overview of all lexers.
     |
     |      The columns are the lexer name, the extensions handled by this lexer
     |      (or "None"), the aliases and a link to the lexer class.
     |
     |  run(self)
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  final_argument_whitespace = False
     |
     |  has_content = False
     |
     |  option_spec = {}
     |
     |  optional_arguments = 0
     |
     |  required_arguments = 1
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from docutils.parsers.rst.Directive:
     |
     |  __init__(self, name, arguments, options, content, lineno, content_offset, block_text, state, state_machine)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  add_name(self, node)
     |      Append self.options['name'] to node['names'] if it exists.
     |
     |      Also normalize the name string and register it as explicit target.
     |
     |  assert_has_content(self)
     |      Throw an ERROR-level DirectiveError if the directive doesn't
     |      have contents.
     |
     |  debug(self, message)
     |
     |  directive_error(self, level, message)
     |      Return a DirectiveError suitable for being thrown as an exception.
     |
     |      Call "raise self.directive_error(level, message)" from within
     |      a directive implementation to return one single system message
     |      at level `level`, which automatically gets the directive block
     |      and the line number added.
     |
     |      Preferably use the `debug`, `info`, `warning`, `error`, or `severe`
     |      wrapper methods, e.g. ``self.error(message)`` to generate an
     |      ERROR-level directive error.
     |
     |  error(self, message)
     |
     |  info(self, message)
     |
     |  severe(self, message)
     |
     |  warning(self, message)
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from docutils.parsers.rst.Directive:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    setup(app)

DATA
    FILTERDOC = '\n.. class:: %s\n\n    :Name: %s\n\n    %s\n\n'
    FMTERDOC = '\n.. class:: %s\n\n    :Short names: %s\n    :Filenames: %...
    LEXERDOC = '\n.. class:: %s\n\n    :Short names: %s\n    :Filenames:  ...
    MODULEDOC = '\n.. module:: %s\n\n%s\n%s\n'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\pygments\sphinxext.py


