Help on module _loop:

NAME
    _loop

FUNCTIONS
    loop_first(values: Iterable[~T]) -> Iterable[Tuple[bool, ~T]]
        Iterate and generate a tuple with a flag for first value.

    loop_first_last(values: Iterable[~T]) -> Iterable[Tuple[bool, bool, ~T]]
        Iterate and generate a tuple with a flag for first and last value.

    loop_last(values: Iterable[~T]) -> Iterable[Tuple[bool, ~T]]
        Iterate and generate a tuple with a flag for last value.

DATA
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    T = ~T
    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\rich\_loop.py


