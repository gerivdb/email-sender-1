Help on module wheel:

NAME
    wheel - Support for installing and building the "wheel" binary package format.

CLASSES
    builtins.object
        ScriptFile
        ZipBackedFile
    pip._internal.exceptions.InstallationError(pip._internal.exceptions.PipError)
        MissingCallableSuffix
    pip._vendor.distlib.scripts.ScriptMaker(builtins.object)
        PipScriptMaker

    class MissingCallableSuffix(pip._internal.exceptions.InstallationError)
     |  MissingCallableSuffix(entry_point: str) -> None
     |
     |  Method resolution order:
     |      MissingCallableSuffix
     |      pip._internal.exceptions.InstallationError
     |      pip._internal.exceptions.PipError
     |      builtins.Exception
     |      builtins.BaseException
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, entry_point: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from pip._internal.exceptions.PipError:
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Static methods inherited from builtins.Exception:
     |
     |  __new__(*args, **kwargs) class method of builtins.Exception
     |      Create and return a new object.  See help(type) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from builtins.BaseException:
     |
     |  __getattribute__(self, name, /)
     |      Return getattr(self, name).
     |
     |  __reduce__(...)
     |      Helper for pickle.
     |
     |  __repr__(self, /)
     |      Return repr(self).
     |
     |  __setstate__(...)
     |
     |  __str__(self, /)
     |      Return str(self).
     |
     |  add_note(...)
     |      Exception.add_note(note) --
     |      add a note to the exception
     |
     |  with_traceback(...)
     |      Exception.with_traceback(tb) --
     |      set self.__traceback__ to tb and return self.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from builtins.BaseException:
     |
     |  __cause__
     |      exception cause
     |
     |  __context__
     |      exception context
     |
     |  __dict__
     |
     |  __suppress_context__
     |
     |  __traceback__
     |
     |  args

    class PipScriptMaker(pip._vendor.distlib.scripts.ScriptMaker)
     |  PipScriptMaker(source_dir, target_dir, add_launchers=True, dry_run=False, fileop=None)
     |
     |  Method resolution order:
     |      PipScriptMaker
     |      pip._vendor.distlib.scripts.ScriptMaker
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  make(self, specification: str, options: Optional[Dict[str, Any]] = None) -> List[str]
     |      Make a script.
     |
     |      :param specification: The specification, which is either a valid export
     |                            entry specification (to make a script from a
     |                            callable) or a filename (to make a script by
     |                            copying from a source location).
     |      :param options: A dictionary of options controlling script generation.
     |      :return: A list of all absolute pathnames written to.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from pip._vendor.distlib.scripts.ScriptMaker:
     |
     |  __init__(self, source_dir, target_dir, add_launchers=True, dry_run=False, fileop=None)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  get_manifest(self, exename)
     |
     |  get_script_filenames(self, name)
     |
     |  make_multiple(self, specifications, options=None)
     |      Take a list of specifications and make scripts from them,
     |      :param specifications: A list of specifications.
     |      :return: A list of all absolute pathnames written to,
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from pip._vendor.distlib.scripts.ScriptMaker:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  dry_run
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes inherited from pip._vendor.distlib.scripts.ScriptMaker:
     |
     |  executable = None
     |
     |  manifest = '<?xml version="1.0" encoding="UTF-8" standalone=...rivileg...
     |
     |  script_template = "# -*- coding: utf-8 -*-\nimport re\nimport sys\nfro...
     |
     |  variant_separator = '-'

    class ScriptFile(builtins.object)
     |  ScriptFile(file: 'File') -> None
     |
     |  Methods defined here:
     |
     |  __init__(self, file: 'File') -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  save(self) -> None
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class ZipBackedFile(builtins.object)
     |  ZipBackedFile(src_record_path: wheel.RecordPath, dest_path: str, zip_file: zipfile.ZipFile) -> None
     |
     |  Methods defined here:
     |
     |  __init__(self, src_record_path: wheel.RecordPath, dest_path: str, zip_file: zipfile.ZipFile) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  save(self) -> None
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    csv_io_kwargs(mode: str) -> Dict[str, Any]
        Return keyword arguments to properly open a CSV file
        in the given mode.

    fix_script(path: str) -> bool
        Replace #!python with #!/path/to/python
        Return True if file was changed.

    get_console_script_specs(console: Dict[str, str]) -> List[str]
        Given the mapping from entrypoint name to callable, return the relevant
        console script specs.

    get_csv_rows_for_installed(old_csv_rows: List[List[str]], installed: Dict[wheel.RecordPath, wheel.RecordPath], changed: Set[wheel.RecordPath], generated: List[str], lib_dir: str) -> List[Tuple[wheel.RecordPath, str, Union[int, str]]]
        :param installed: A map from archive RECORD path to installation RECORD
            path.

    get_entrypoints(dist: pip._internal.metadata.base.BaseDistribution) -> Tuple[Dict[str, str], Dict[str, str]]

    install_wheel(name: str, wheel_path: str, scheme: pip._internal.models.scheme.Scheme, req_description: str, pycompile: bool = True, warn_script_location: bool = True, direct_url: Optional[pip._internal.models.direct_url.DirectUrl] = None, requested: bool = False) -> None

    message_about_scripts_not_on_PATH(scripts: Sequence[str]) -> Optional[str]
        Determine if any scripts are not on PATH and format a warning.
        Returns a warning message if one or more scripts are not on PATH,
        otherwise None.

    rehash(path: str, blocksize: int = 1048576) -> Tuple[str, str]
        Return (encoded_digest, length) for path using hashlib.sha256()

    req_error_context(req_description: str) -> Generator[NoneType, NoneType, NoneType]

    wheel_root_is_purelib(metadata: email.message.Message) -> bool

DATA
    Callable = typing.Callable
        Deprecated alias to collections.abc.Callable.

        Callable[[int], str] signifies a function that takes a single
        parameter of type int and returns a str.

        The subscription syntax must always be used with exactly two
        values: the argument list and the return type.
        The argument list must be a list of types, a ParamSpec,
        Concatenate or ellipsis. The return type must be a single type.

        There is no syntax to indicate optional or keyword arguments;
        such function types are rarely used as callback types.

    DIRECT_URL_METADATA_NAME = 'direct_url.json'
    Dict = typing.Dict
        A generic version of dict.

    Generator = typing.Generator
        A generic version of collections.abc.Generator.

    InstalledCSVRow = typing.Tuple[wheel.RecordPath, str, typing.Union[int...
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    Iterator = typing.Iterator
        A generic version of collections.abc.Iterator.

    List = typing.List
        A generic version of list.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    RecordPath = wheel.RecordPath
    SCHEME_KEYS = ['platlib', 'purelib', 'headers', 'scripts', 'data']
    Sequence = typing.Sequence
        A generic version of collections.abc.Sequence.

    Set = typing.Set
        A generic version of set.

    TYPE_CHECKING = False
    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    Union = typing.Union
        Union type; Union[X, Y] means either X or Y.

        On Python 3.10 and higher, the | operator
        can also be used to denote unions;
        X | Y means the same thing to the type checker as Union[X, Y].

        To define a union, use e.g. Union[int, str]. Details:
        - The arguments must be types and there must be at least one.
        - None as an argument is a special case and is replaced by
          type(None).
        - Unions of unions are flattened, e.g.::

            assert Union[Union[int, str], float] == Union[int, str, float]

        - Unions of a single argument vanish, e.g.::

            assert Union[int] == int  # The constructor actually returns int

        - Redundant arguments are skipped, e.g.::

            assert Union[int, str, int] == Union[int, str]

        - When comparing unions, the argument order is ignored, e.g.::

            assert Union[int, str] == Union[str, int]

        - You cannot subclass or instantiate a union.
        - You can use Optional[X] as a shorthand for Union[X, None].

    logger = <VerboseLogger wheel (WARNING)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\operations\install\wheel.py


