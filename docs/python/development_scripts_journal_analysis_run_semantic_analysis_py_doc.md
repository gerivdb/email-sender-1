Help on module run_semantic_analysis:

NAME
    run_semantic_analysis

FUNCTIONS
    detect_patterns()
        Détecte les patterns dans les analyses sémantiques.

    main()

    run_all_analyses()
        Exécute toutes les analyses sémantiques.

    run_embeddings_generation()
        Génère les embeddings pour toutes les entrées du journal.

    run_sentiment_analysis()
        Analyse le sentiment de toutes les entrées du journal.

    run_topic_modeling(model='lda')
        Modélise les sujets des entrées du journal.

DATA
    logger = <Logger run_semantic_analysis (INFO)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\development\scripts\journal\analysis\run_semantic_analysis.py


