Help on module sources:

NAME
    sources

CLASSES
    builtins.object
        LinkSource

    class LinkSource(builtins.object)
     |  Methods defined here:
     |
     |  file_links(self) -> Iterable[pip._internal.models.link.Link]
     |      Links found by specifying archives directly.
     |
     |  page_candidates(self) -> Iterable[pip._internal.models.candidate.InstallationCandidate]
     |      Candidates found by parsing an archive listing HTML file.
     |
     |  ----------------------------------------------------------------------
     |  Readonly properties defined here:
     |
     |  link
     |      Returns the underlying link, if there's one.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    build_source(location: str, *, candidates_from_page: Callable[[pip._internal.models.link.Link], Iterable[pip._internal.models.candidate.InstallationCandidate]], page_validator: Callable[[pip._internal.models.link.Link], bool], expand_dir: bool, cache_link_parsing: bool, project_name: str) -> Tuple[Optional[str], Optional[sources.LinkSource]]

DATA
    Callable = typing.Callable
        Deprecated alias to collections.abc.Callable.

        Callable[[int], str] signifies a function that takes a single
        parameter of type int and returns a str.

        The subscription syntax must always be used with exactly two
        values: the argument list and the return type.
        The argument list must be a list of types, a ParamSpec,
        Concatenate or ellipsis. The return type must be a single type.

        There is no syntax to indicate optional or keyword arguments;
        such function types are rarely used as callback types.

    CandidatesFromPage = typing.Callable[[pip._internal.models.link.Link]....
    Dict = typing.Dict
        A generic version of dict.

    FoundCandidates = typing.Iterable[pip._internal.models.candidate.Insta...
    FoundLinks = typing.Iterable[pip._internal.models.link.Link]
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    List = typing.List
        A generic version of list.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    PageValidator = typing.Callable[[pip._internal.models.link.Link], bool...
    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    logger = <VerboseLogger sources (WARNING)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\index\sources.py


