Help on module cache:

NAME
    cache - Cache Management

CLASSES
    builtins.object
        Cache
            SimpleWheelCache
                EphemWheelCache
            WheelCache
        CacheEntry

    class Cache(builtins.object)
     |  Cache(cache_dir: str) -> None
     |
     |  An abstract class - provides cache directories for data from links
     |
     |  :param cache_dir: The root of the cache.
     |
     |  Methods defined here:
     |
     |  __init__(self, cache_dir: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  get(self, link: pip._internal.models.link.Link, package_name: Optional[str], supported_tags: List[pip._vendor.packaging.tags.Tag]) -> pip._internal.models.link.Link
     |      Returns a link to a cached item if it exists, otherwise returns the
     |      passed link.
     |
     |  get_path_for_link(self, link: pip._internal.models.link.Link) -> str
     |      Return a directory to store cached items in for link.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class CacheEntry(builtins.object)
     |  CacheEntry(link: pip._internal.models.link.Link, persistent: bool)
     |
     |  Methods defined here:
     |
     |  __init__(self, link: pip._internal.models.link.Link, persistent: bool)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class EphemWheelCache(SimpleWheelCache)
     |  EphemWheelCache() -> None
     |
     |  A SimpleWheelCache that creates it's own temporary cache directory
     |
     |  Method resolution order:
     |      EphemWheelCache
     |      SimpleWheelCache
     |      Cache
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from SimpleWheelCache:
     |
     |  get(self, link: pip._internal.models.link.Link, package_name: Optional[str], supported_tags: List[pip._vendor.packaging.tags.Tag]) -> pip._internal.models.link.Link
     |      Returns a link to a cached item if it exists, otherwise returns the
     |      passed link.
     |
     |  get_path_for_link(self, link: pip._internal.models.link.Link) -> str
     |      Return a directory to store cached wheels for link
     |
     |      Because there are M wheels for any one sdist, we provide a directory
     |      to cache them in, and then consult that directory when looking up
     |      cache hits.
     |
     |      We only insert things into the cache if they have plausible version
     |      numbers, so that we don't contaminate the cache with things that were
     |      not unique. E.g. ./package might have dozens of installs done for it
     |      and build a version of 0.0...and if we built and cached a wheel, we'd
     |      end up using the same wheel even if the source has been edited.
     |
     |      :param link: The link of the sdist for which this will cache wheels.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from Cache:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class SimpleWheelCache(Cache)
     |  SimpleWheelCache(cache_dir: str) -> None
     |
     |  A cache of wheels for future installs.
     |
     |  Method resolution order:
     |      SimpleWheelCache
     |      Cache
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, cache_dir: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  get(self, link: pip._internal.models.link.Link, package_name: Optional[str], supported_tags: List[pip._vendor.packaging.tags.Tag]) -> pip._internal.models.link.Link
     |      Returns a link to a cached item if it exists, otherwise returns the
     |      passed link.
     |
     |  get_path_for_link(self, link: pip._internal.models.link.Link) -> str
     |      Return a directory to store cached wheels for link
     |
     |      Because there are M wheels for any one sdist, we provide a directory
     |      to cache them in, and then consult that directory when looking up
     |      cache hits.
     |
     |      We only insert things into the cache if they have plausible version
     |      numbers, so that we don't contaminate the cache with things that were
     |      not unique. E.g. ./package might have dozens of installs done for it
     |      and build a version of 0.0...and if we built and cached a wheel, we'd
     |      end up using the same wheel even if the source has been edited.
     |
     |      :param link: The link of the sdist for which this will cache wheels.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from Cache:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class WheelCache(Cache)
     |  WheelCache(cache_dir: str) -> None
     |
     |  Wraps EphemWheelCache and SimpleWheelCache into a single Cache
     |
     |  This Cache allows for gracefully degradation, using the ephem wheel cache
     |  when a certain link is not found in the simple wheel cache first.
     |
     |  Method resolution order:
     |      WheelCache
     |      Cache
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, cache_dir: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  get(self, link: pip._internal.models.link.Link, package_name: Optional[str], supported_tags: List[pip._vendor.packaging.tags.Tag]) -> pip._internal.models.link.Link
     |      Returns a link to a cached item if it exists, otherwise returns the
     |      passed link.
     |
     |  get_cache_entry(self, link: pip._internal.models.link.Link, package_name: Optional[str], supported_tags: List[pip._vendor.packaging.tags.Tag]) -> Optional[cache.CacheEntry]
     |      Returns a CacheEntry with a link to a cached item if it exists or
     |      None. The cache entry indicates if the item was found in the persistent
     |      or ephemeral cache.
     |
     |  get_ephem_path_for_link(self, link: pip._internal.models.link.Link) -> str
     |
     |  get_path_for_link(self, link: pip._internal.models.link.Link) -> str
     |      Return a directory to store cached items in for link.
     |
     |  ----------------------------------------------------------------------
     |  Static methods defined here:
     |
     |  record_download_origin(cache_dir: str, download_info: pip._internal.models.direct_url.DirectUrl) -> None
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from Cache:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

DATA
    Dict = typing.Dict
        A generic version of dict.

    List = typing.List
        A generic version of list.

    ORIGIN_JSON_NAME = 'origin.json'
    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    logger = <VerboseLogger cache (WARNING)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\cache.py


