Help on module setup:

NAME
    setup

FUNCTIONS
    build_rag_index()
        Construit l'index RAG.

    build_search_index()
        Construit l'index de recherche.

    check_dependencies()
        Vérifie si les dépendances sont installées.

    create_directories()
        Crée les répertoires nécessaires.

    create_sample_entry()
        Crée une entrée d'exemple.

    migrate_journal()
        Migre le journal existant.

    setup_all()
        Configure tout le système.

    setup_git_hooks()
        Configure les hooks Git.

    setup_vscode()
        Configure VS Code.

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\development\scripts\journal\web\setup.py


