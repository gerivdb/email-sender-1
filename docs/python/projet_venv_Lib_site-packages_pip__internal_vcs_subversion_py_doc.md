Help on module subversion:

NAME
    subversion

CLASSES
    pip._internal.vcs.versioncontrol.VersionControl(builtins.object)
        Subversion

    class Subversion(pip._internal.vcs.versioncontrol.VersionControl)
     |  Subversion(use_interactive: Optional[bool] = None) -> None
     |
     |  Method resolution order:
     |      Subversion
     |      pip._internal.vcs.versioncontrol.VersionControl
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, use_interactive: Optional[bool] = None) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  call_vcs_version(self) -> Tuple[int, ...]
     |      Query the version of the currently installed Subversion client.
     |
     |      :return: A tuple containing the parts of the version information or
     |          ``()`` if the version returned from ``svn`` could not be parsed.
     |      :raises: BadCommand: If ``svn`` is not installed.
     |
     |  fetch_new(self, dest: str, url: pip._internal.utils.misc.HiddenText, rev_options: pip._internal.vcs.versioncontrol.RevOptions, verbosity: int) -> None
     |      Fetch a revision from a repository, in the case that this is the
     |      first fetch from the repository.
     |
     |      Args:
     |        dest: the directory to fetch the repository to.
     |        rev_options: a RevOptions object.
     |        verbosity: verbosity level.
     |
     |  get_remote_call_options(self) -> List[Union[str, pip._internal.utils.misc.HiddenText]]
     |      Return options to be used on calls to Subversion that contact the server.
     |
     |      These options are applicable for the following ``svn`` subcommands used
     |      in this class.
     |
     |          - checkout
     |          - switch
     |          - update
     |
     |      :return: A list of command line arguments to pass to ``svn``.
     |
     |  get_vcs_version(self) -> Tuple[int, ...]
     |      Return the version of the currently installed Subversion client.
     |
     |      If the version of the Subversion client has already been queried,
     |      a cached value will be used.
     |
     |      :return: A tuple containing the parts of the version information or
     |          ``()`` if the version returned from ``svn`` could not be parsed.
     |      :raises: BadCommand: If ``svn`` is not installed.
     |
     |  switch(self, dest: str, url: pip._internal.utils.misc.HiddenText, rev_options: pip._internal.vcs.versioncontrol.RevOptions) -> None
     |      Switch the repo at ``dest`` to point to ``URL``.
     |
     |      Args:
     |        rev_options: a RevOptions object.
     |
     |  update(self, dest: str, url: pip._internal.utils.misc.HiddenText, rev_options: pip._internal.vcs.versioncontrol.RevOptions) -> None
     |      Update an already-existing repo to the given ``rev_options``.
     |
     |      Args:
     |        rev_options: a RevOptions object.
     |
     |  ----------------------------------------------------------------------
     |  Class methods defined here:
     |
     |  get_netloc_and_auth(netloc: str, scheme: str) -> Tuple[str, Tuple[Optional[str], Optional[str]]]
     |      This override allows the auth information to be passed to svn via the
     |      --username and --password options instead of via the URL.
     |
     |  get_remote_url(location: str) -> str
     |      Return the url used at location
     |
     |      Raises RemoteNotFoundError if the repository does not have a remote
     |      url configured.
     |
     |  get_revision(location: str) -> str
     |      Return the maximum revision for all files under a given location
     |
     |  get_url_rev_and_auth(url: str) -> Tuple[str, Optional[str], Tuple[Optional[str], Optional[str]]]
     |      Parse the repository URL to use, and return the URL, revision,
     |      and auth info to use.
     |
     |      Returns: (url, rev, (username, password)).
     |
     |  is_commit_id_equal(dest: str, name: Optional[str]) -> bool
     |      Always assume the versions don't match
     |
     |  should_add_vcs_url_prefix(remote_url: str) -> bool
     |      Return whether the vcs prefix (e.g. "git+") should be added to a
     |      repository's remote url when used in a requirement.
     |
     |  ----------------------------------------------------------------------
     |  Static methods defined here:
     |
     |  get_base_rev_args(rev: str) -> List[str]
     |      Return the base revision arguments for a vcs command.
     |
     |      Args:
     |        rev: the name of a revision to install.  Cannot be None.
     |
     |  make_rev_args(username: Optional[str], password: Optional[pip._internal.utils.misc.HiddenText]) -> List[Union[str, pip._internal.utils.misc.HiddenText]]
     |      Return the RevOptions "extra arguments" to use in obtain().
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  __annotations__ = {}
     |
     |  dirname = '.svn'
     |
     |  name = 'svn'
     |
     |  repo_name = 'checkout'
     |
     |  schemes = ('svn+ssh', 'svn+http', 'svn+https', 'svn+svn', 'svn+file')
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from pip._internal.vcs.versioncontrol.VersionControl:
     |
     |  get_url_rev_options(self, url: pip._internal.utils.misc.HiddenText) -> Tuple[pip._internal.utils.misc.HiddenText, pip._internal.vcs.versioncontrol.RevOptions]
     |      Return the URL and RevOptions object to use in obtain(),
     |      as a tuple (url, rev_options).
     |
     |  is_immutable_rev_checkout(self, url: str, dest: str) -> bool
     |      Return true if the commit hash checked out at dest matches
     |      the revision in url.
     |
     |      Always return False, if the VCS does not support immutable commit
     |      hashes.
     |
     |      This method does not check if there are local uncommitted changes
     |      in dest after checkout, as pip currently has no use case for that.
     |
     |  obtain(self, dest: str, url: pip._internal.utils.misc.HiddenText, verbosity: int) -> None
     |      Install or update in editable mode the package represented by this
     |      VersionControl object.
     |
     |      :param dest: the repository directory in which to install or update.
     |      :param url: the repository URL starting with a vcs prefix.
     |      :param verbosity: verbosity level.
     |
     |  unpack(self, location: str, url: pip._internal.utils.misc.HiddenText, verbosity: int) -> None
     |      Clean up current location and download the url repository
     |      (and vcs infos) into location
     |
     |      :param url: the repository URL starting with a vcs prefix.
     |      :param verbosity: verbosity level.
     |
     |  ----------------------------------------------------------------------
     |  Class methods inherited from pip._internal.vcs.versioncontrol.VersionControl:
     |
     |  compare_urls(url1: str, url2: str) -> bool
     |      Compare two repo URLs for identity, ignoring incidental differences.
     |
     |  get_repository_root(location: str) -> Optional[str]
     |      Return the "root" (top-level) directory controlled by the vcs,
     |      or `None` if the directory is not in any.
     |
     |      It is meant to be overridden to implement smarter detection
     |      mechanisms for specific vcs.
     |
     |      This can do more than is_repository_directory() alone. For
     |      example, the Git override checks that Git is actually available.
     |
     |  get_requirement_revision(repo_dir: str) -> str
     |      Return the revision string that should be used in a requirement.
     |
     |  get_src_requirement(repo_dir: str, project_name: str) -> str
     |      Return the requirement string to use to redownload the files
     |      currently at the given repository directory.
     |
     |      Args:
     |        project_name: the (unescaped) project name.
     |
     |      The return value has a form similar to the following:
     |
     |          {repository_url}@{revision}#egg={project_name}
     |
     |  get_subdirectory(location: str) -> Optional[str]
     |      Return the path to Python project root, relative to the repo root.
     |      Return None if the project root is in the repo root.
     |
     |  is_repository_directory(path: str) -> bool
     |      Return whether a directory path is a repository directory.
     |
     |  make_rev_options(rev: Optional[str] = None, extra_args: Optional[List[Union[str, pip._internal.utils.misc.HiddenText]]] = None) -> pip._internal.vcs.versioncontrol.RevOptions
     |      Return a RevOptions object.
     |
     |      Args:
     |        rev: the name of a revision to install.
     |        extra_args: a list of extra options.
     |
     |  run_command(cmd: Union[List[str], List[Union[str, pip._internal.utils.misc.HiddenText]]], show_stdout: bool = True, cwd: Optional[str] = None, on_returncode: 'Literal["raise", "warn", "ignore"]' = 'raise', extra_ok_returncodes: Optional[Iterable[int]] = None, command_desc: Optional[str] = None, extra_environ: Optional[Mapping[str, Any]] = None, spinner: Optional[pip._internal.cli.spinners.SpinnerInterface] = None, log_failed_cmd: bool = True, stdout_only: bool = False) -> str
     |      Run a VCS subcommand
     |      This is simply a wrapper around call_subprocess that adds the VCS
     |      command name, and checks that the VCS is available
     |
     |  ----------------------------------------------------------------------
     |  Static methods inherited from pip._internal.vcs.versioncontrol.VersionControl:
     |
     |  normalize_url(url: str) -> str
     |      Normalize a URL for comparison by unquoting it and removing any
     |      trailing slash.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from pip._internal.vcs.versioncontrol.VersionControl:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes inherited from pip._internal.vcs.versioncontrol.VersionControl:
     |
     |  default_arg_rev = None
     |
     |  unset_environ = ()

DATA
    AuthInfo = typing.Tuple[typing.Optional[str], typing.Optional[str]]
    CommandArgs = typing.List[typing.Union[str, pip._internal.utils.misc.H...
    List = typing.List
        A generic version of list.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    logger = <VerboseLogger subversion (WARNING)>
    vcs = <pip._internal.vcs.versioncontrol.VcsSupport object>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\vcs\subversion.py


