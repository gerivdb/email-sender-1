Help on module run_app:

NAME
    run_app

FUNCTIONS
    build_rag_index()
        Construit l'index RAG.

    check_environment()
        Vérifie que l'environnement est correctement configuré.

    run_server(host='0.0.0.0', port=8000, reload=True)
        Lance le serveur FastAPI.

DATA
    logger = <Logger run_app (INFO)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\development\scripts\journal\web\run_app.py


