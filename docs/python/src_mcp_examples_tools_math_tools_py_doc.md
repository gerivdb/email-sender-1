Help on module math_tools:

NAME
    math_tools - Module contenant des outils mathématiques pour le MCP.

DESCRIPTION
    Ce module contient des outils mathématiques qui peuvent être découverts
    et utilisés par le ToolsManager.

FUNCTIONS
    add(a: int, b: int) -> int
        Additionne deux nombres.

        Args:
            a (int): Premier nombre
            b (int): Deuxième nombre

        Returns:
            int: Somme des deux nombres

    divide(a: float, b: float) -> float
        Divise deux nombres.

        Args:
            a (float): Numérateur
            b (float): Dénominateur

        Returns:
            float: Résultat de la division

        Raises:
            ZeroDivisionError: Si le dénominateur est zéro

    mul(a: int, b: int) -> int
        Multiplie deux nombres.

        Args:
            a (int): Premier nombre
            b (int): Deuxième nombre

        Returns:
            int: Produit des deux nombres

    subtract(a: int, b: int) -> int
        Soustrait deux nombres.

        Args:
            a (int): Premier nombre
            b (int): Deuxième nombre

        Returns:
            int: Différence des deux nombres

DATA
    parent_dir = r'D:\DO\WEB\N8N_tests\PROJETS\EMAIL_SENDER_1'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\src\mcp\examples\tools\math_tools.py


