Help on module base:

NAME
    base

CLASSES
    builtins.object
        AbstractDistribution

    class AbstractDistribution(builtins.object)
     |  AbstractDistribution(req: pip._internal.req.req_install.InstallRequirement) -> None
     |
     |  A base class for handling installable artifacts.
     |
     |  The requirements for anything installable are as follows:
     |
     |   - we must be able to determine the requirement name
     |     (or we can't correctly handle the non-upgrade case).
     |
     |   - for packages with setup requirements, we must also be able
     |     to determine their requirements without installing additional
     |     packages (for the same reason as run-time dependencies)
     |
     |   - we must be able to create a Distribution object exposing the
     |     above metadata.
     |
     |   - if we need to do work in the build tracker, we must be able to generate a unique
     |     string to identify the requirement in the build tracker.
     |
     |  Methods defined here:
     |
     |  __init__(self, req: pip._internal.req.req_install.InstallRequirement) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  get_metadata_distribution(self) -> pip._internal.metadata.base.BaseDistribution
     |
     |  prepare_distribution_metadata(self, finder: 'PackageFinder', build_isolation: bool, check_build_deps: bool) -> None
     |
     |  ----------------------------------------------------------------------
     |  Readonly properties defined here:
     |
     |  build_tracker_id
     |      A string that uniquely identifies this requirement to the build tracker.
     |
     |      If None, then this dist has no work to do in the build tracker, and
     |      ``.prepare_distribution_metadata()`` will not be called.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  __abstractmethods__ = frozenset({'build_tracker_id', 'get_metadata_dis...

DATA
    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    TYPE_CHECKING = False

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\distributions\base.py


