Help on module filetypes:

NAME
    filetypes - Filetype information.

FUNCTIONS
    is_archive_file(name: str) -> bool
        Return True if `name` is a considered as an archive file.

DATA
    ARCHIVE_EXTENSIONS = ('.zip', '.whl', '.tar.bz2', '.tbz', '.tar.gz', '...
    BZ2_EXTENSIONS = ('.tar.bz2', '.tbz')
    TAR_EXTENSIONS = ('.tar.gz', '.tgz', '.tar')
    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    WHEEL_EXTENSION = '.whl'
    XZ_EXTENSIONS = ('.tar.xz', '.txz', '.tlz', '.tar.lz', '.tar.lzma')
    ZIP_EXTENSIONS = ('.zip', '.whl')
    __annotations__ = {'BZ2_EXTENSIONS': typing.Tuple[str, ...], 'TAR_EXTE...

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\utils\filetypes.py


