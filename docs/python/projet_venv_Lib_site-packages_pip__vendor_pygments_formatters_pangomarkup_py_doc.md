Help on module pangomarkup:

NAME
    pangomarkup

DESCRIPTION
    pygments.formatters.pangomarkup
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

    Formatter for Pango markup output.

    :copyright: Copyright 2006-2024 by the Pygments team, see AUTHORS.
    :license: BSD, see LICENSE for details.

CLASSES
    pip._vendor.pygments.formatter.Formatter(builtins.object)
        PangoMarkupFormatter

    class PangoMarkupFormatter(pip._vendor.pygments.formatter.Formatter)
     |  PangoMarkupFormatter(**options)
     |
     |  Format tokens as Pango Markup code. It can then be rendered to an SVG.
     |
     |  .. versionadded:: 2.9
     |
     |  Method resolution order:
     |      PangoMarkupFormatter
     |      pip._vendor.pygments.formatter.Formatter
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, **options)
     |      As with lexers, this constructor takes arbitrary optional arguments,
     |      and if you override it, you should first process your own options, then
     |      call the base class implementation.
     |
     |  format_unencoded(self, tokensource, outfile)
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  aliases = ['pango', 'pangomarkup']
     |
     |  filenames = []
     |
     |  name = 'Pango Markup'
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from pip._vendor.pygments.formatter.Formatter:
     |
     |  format(self, tokensource, outfile)
     |      This method must format the tokens from the `tokensource` iterable and
     |      write the formatted version to the file object `outfile`.
     |
     |      Formatter options can control how exactly the tokens are converted.
     |
     |  get_style_defs(self, arg='')
     |      This method must return statements or declarations suitable to define
     |      the current style for subsequent highlighted text (e.g. CSS classes
     |      in the `HTMLFormatter`).
     |
     |      The optional argument `arg` can be used to modify the generation and
     |      is formatter dependent (it is standardized because it can be given on
     |      the command line).
     |
     |      This method is called by the ``-S`` :doc:`command-line option <cmdline>`,
     |      the `arg` is then given by the ``-a`` option.
     |
     |  ----------------------------------------------------------------------
     |  Class methods inherited from pip._vendor.pygments.formatter.Formatter:
     |
     |  __class_getitem__(name)
     |      # Allow writing Formatter[str] or Formatter[bytes]. That's equivalent to
     |      # Formatter. This helps when using third-party type stubs from typeshed.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from pip._vendor.pygments.formatter.Formatter:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes inherited from pip._vendor.pygments.formatter.Formatter:
     |
     |  unicodeoutput = True

DATA
    __all__ = ['PangoMarkupFormatter']

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\pygments\formatters\pangomarkup.py


