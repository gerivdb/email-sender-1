Help on module hashes:

NAME
    hashes

CLASSES
    builtins.object
        Hashes
            MissingHashes

    class Hashes(builtins.object)
     |  Hashes(hashes: Optional[Dict[str, List[str]]] = None) -> None
     |
     |  A wrapper that builds multiple hashes at once and checks them against
     |  known-good values
     |
     |  Methods defined here:
     |
     |  __and__(self, other: 'Hashes') -> 'Hashes'
     |
     |  __bool__(self) -> bool
     |      Return whether I know any known-good hashes.
     |
     |  __eq__(self, other: object) -> bool
     |      Return self==value.
     |
     |  __hash__(self) -> int
     |      Return hash(self).
     |
     |  __init__(self, hashes: Optional[Dict[str, List[str]]] = None) -> None
     |      :param hashes: A dict of algorithm names pointing to lists of allowed
     |          hex digests
     |
     |  check_against_chunks(self, chunks: Iterable[bytes]) -> None
     |      Check good hashes against ones built from iterable of chunks of
     |      data.
     |
     |      Raise HashMismatch if none match.
     |
     |  check_against_file(self, file: <class 'BinaryIO'>) -> None
     |      Check good hashes against a file-like object
     |
     |      Raise HashMismatch if none match.
     |
     |  check_against_path(self, path: str) -> None
     |
     |  has_one_of(self, hashes: Dict[str, str]) -> bool
     |      Return whether any of the given hashes are allowed.
     |
     |  is_hash_allowed(self, hash_name: str, hex_digest: str) -> bool
     |      Return whether the given hex digest is allowed.
     |
     |  ----------------------------------------------------------------------
     |  Readonly properties defined here:
     |
     |  digest_count
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class MissingHashes(Hashes)
     |  MissingHashes() -> None
     |
     |  A workalike for Hashes used when we're missing a hash for a requirement
     |
     |  It computes the actual hash of the requirement and raises a HashMissing
     |  exception showing it to the user.
     |
     |  Method resolution order:
     |      MissingHashes
     |      Hashes
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self) -> None
     |      Don't offer the ``hashes`` kwarg.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from Hashes:
     |
     |  __and__(self, other: 'Hashes') -> 'Hashes'
     |
     |  __bool__(self) -> bool
     |      Return whether I know any known-good hashes.
     |
     |  __eq__(self, other: object) -> bool
     |      Return self==value.
     |
     |  __hash__(self) -> int
     |      Return hash(self).
     |
     |  check_against_chunks(self, chunks: Iterable[bytes]) -> None
     |      Check good hashes against ones built from iterable of chunks of
     |      data.
     |
     |      Raise HashMismatch if none match.
     |
     |  check_against_file(self, file: <class 'BinaryIO'>) -> None
     |      Check good hashes against a file-like object
     |
     |      Raise HashMismatch if none match.
     |
     |  check_against_path(self, path: str) -> None
     |
     |  has_one_of(self, hashes: Dict[str, str]) -> bool
     |      Return whether any of the given hashes are allowed.
     |
     |  is_hash_allowed(self, hash_name: str, hex_digest: str) -> bool
     |      Return whether the given hex digest is allowed.
     |
     |  ----------------------------------------------------------------------
     |  Readonly properties inherited from Hashes:
     |
     |  digest_count
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from Hashes:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

DATA
    Dict = typing.Dict
        A generic version of dict.

    FAVORITE_HASH = 'sha256'
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    List = typing.List
        A generic version of list.

    NoReturn = typing.NoReturn
        Special type indicating functions that never return.

        Example::

            from typing import NoReturn

            def stop() -> NoReturn:
                raise Exception('no way')

        NoReturn can also be used as a bottom type, a type that
        has no values. Starting in Python 3.11, the Never type should
        be used for this concept instead. Type checkers should treat the two
        equivalently.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    STRONG_HASHES = ['sha256', 'sha384', 'sha512']
    TYPE_CHECKING = False

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\utils\hashes.py


