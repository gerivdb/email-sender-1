Help on module token:

NAME
    token

DESCRIPTION
    pygments.token
    ~~~~~~~~~~~~~~

    Basic token types and the standard tokens.

    :copyright: Copyright 2006-2024 by the Pygments team, see AUTHORS.
    :license: BSD, see LICENSE for details.

FUNCTIONS
    is_token_subtype(ttype, other)
        Return True if ``ttype`` is a subtype of ``other``.

        exists for backwards compatibility. use ``ttype in other`` now.

    string_to_tokentype(s)
        Convert a string into a token type::

            >>> string_to_token('String.Double')
            Token.Literal.String.Double
            >>> string_to_token('Token.Literal.Number')
            Token.Literal.Number
            >>> string_to_token('')
            Token

        Tokens that are already tokens are returned unchanged:

            >>> string_to_token(String)
            Token.Literal.String

DATA
    Comment = Token.Comment
    Error = Token.Error
    Escape = Token.Escape
    Generic = Token.Generic
    Keyword = Token.Keyword
    Literal = Token.Literal
    Name = Token.Name
    Number = Token.Literal.Number
    Operator = Token.Operator
    Other = Token.Other
    Punctuation = Token.Punctuation
    STANDARD_TYPES = {Token: '', Token.Comment: 'c', Token.Comment.Hashban...
    String = Token.Literal.String
    Text = Token.Text
    Token = Token
    Whitespace = Token.Text.Whitespace

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\pygments\token.py


