Help on module test_selection:

NAME
    test_selection - Test pour les fonctionnalités de sélection.

FUNCTIONS
    create_test_data(storage_path)
        Crée des données de test.

    main()
        Point d'entrée principal des tests.

    print_header(title)
        Affiche un en-tête de section.

    print_result(test_name, success)
        Affiche le résultat d'un test.

    test_manager_selection(storage_path, archive_path, items)
        Teste les fonctionnalités de sélection du gestionnaire CRUD.

    test_selector_by_content(storage_path, items)
        Teste la sélection par contenu.

    test_selector_by_id(storage_path, items)
        Teste la sélection par ID.

    test_selector_by_metadata(storage_path, items)
        Teste la sélection par métadonnées.

    test_selector_by_theme(storage_path, items)
        Teste la sélection par thème.

    test_selector_combine(storage_path, items)
        Teste la combinaison de sélections.

DATA
    parent_dir = r'D:\DO\WEB\N8N_tests\PROJETS\EMAIL_SENDER_1'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\src\orchestrator\test_selection.py


