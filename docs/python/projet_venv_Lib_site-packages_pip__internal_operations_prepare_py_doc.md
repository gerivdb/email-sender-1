Help on module prepare:

NAME
    prepare - Prepares a distribution for installation

CLASSES
    builtins.object
        File
        RequirementPreparer

    class File(builtins.object)
     |  File(path: str, content_type: Optional[str] = None) -> None
     |
     |  File(path: str, content_type: Optional[str] = None)
     |
     |  Methods defined here:
     |
     |  __eq__(self, other)
     |      Return self==value.
     |
     |  __init__(self, path: str, content_type: Optional[str] = None) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  __post_init__(self) -> None
     |
     |  __repr__(self)
     |      Return repr(self).
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  __annotations__ = {'content_type': typing.Optional[str], 'path': <clas...
     |
     |  __dataclass_fields__ = {'content_type': Field(name='content_type',type...
     |
     |  __dataclass_params__ = _DataclassParams(init=True,repr=True,eq=True,or...
     |
     |  __hash__ = None
     |
     |  __match_args__ = ('path', 'content_type')
     |
     |  content_type = None

    class RequirementPreparer(builtins.object)
     |  RequirementPreparer(build_dir: str, download_dir: Optional[str], src_dir: str, build_isolation: bool, check_build_deps: bool, build_tracker: pip._internal.operations.build.build_tracker.BuildTracker, session: pip._internal.network.session.PipSession, progress_bar: str, finder: pip._internal.index.package_finder.PackageFinder, require_hashes: bool, use_user_site: bool, lazy_wheel: bool, verbosity: int, legacy_resolver: bool) -> None
     |
     |  Prepares a Requirement
     |
     |  Methods defined here:
     |
     |  __init__(self, build_dir: str, download_dir: Optional[str], src_dir: str, build_isolation: bool, check_build_deps: bool, build_tracker: pip._internal.operations.build.build_tracker.BuildTracker, session: pip._internal.network.session.PipSession, progress_bar: str, finder: pip._internal.index.package_finder.PackageFinder, require_hashes: bool, use_user_site: bool, lazy_wheel: bool, verbosity: int, legacy_resolver: bool) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  prepare_editable_requirement(self, req: pip._internal.req.req_install.InstallRequirement) -> pip._internal.metadata.base.BaseDistribution
     |      Prepare an editable requirement.
     |
     |  prepare_installed_requirement(self, req: pip._internal.req.req_install.InstallRequirement, skip_reason: str) -> pip._internal.metadata.base.BaseDistribution
     |      Prepare an already-installed requirement.
     |
     |  prepare_linked_requirement(self, req: pip._internal.req.req_install.InstallRequirement, parallel_builds: bool = False) -> pip._internal.metadata.base.BaseDistribution
     |      Prepare a requirement to be obtained from req.link.
     |
     |  prepare_linked_requirements_more(self, reqs: Iterable[pip._internal.req.req_install.InstallRequirement], parallel_builds: bool = False) -> None
     |      Prepare linked requirements more, if needed.
     |
     |  save_linked_requirement(self, req: pip._internal.req.req_install.InstallRequirement) -> None
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    get_file_url(link: pip._internal.models.link.Link, download_dir: Optional[str] = None, hashes: Optional[pip._internal.utils.hashes.Hashes] = None) -> prepare.File
        Get file and optionally check its hash.

    get_http_url(link: pip._internal.models.link.Link, download: pip._internal.network.download.Downloader, download_dir: Optional[str] = None, hashes: Optional[pip._internal.utils.hashes.Hashes] = None) -> prepare.File

    unpack_url(link: pip._internal.models.link.Link, location: str, download: pip._internal.network.download.Downloader, verbosity: int, download_dir: Optional[str] = None, hashes: Optional[pip._internal.utils.hashes.Hashes] = None) -> Optional[prepare.File]
        Unpack link into location, downloading if required.

        :param hashes: A Hashes object, one of whose embedded hashes must match,
            or HashMismatch will be raised. If the Hashes is empty, no matches are
            required, and unhashable types of requirements (like VCS ones, which
            would ordinarily raise HashUnsupported) are allowed.

    unpack_vcs_link(link: pip._internal.models.link.Link, location: str, verbosity: int) -> None

DATA
    Dict = typing.Dict
        A generic version of dict.

    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    List = typing.List
        A generic version of list.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    logger = <VerboseLogger prepare (WARNING)>
    vcs = <pip._internal.vcs.versioncontrol.VcsSupport object>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\operations\prepare.py


