Help on module download:

NAME
    download - Download files with progress indicators.

CLASSES
    builtins.object
        BatchDownloader
        Downloader

    class BatchDownloader(builtins.object)
     |  BatchDownloader(session: pip._internal.network.session.PipSession, progress_bar: str) -> None
     |
     |  Methods defined here:
     |
     |  __call__(self, links: Iterable[pip._internal.models.link.Link], location: str) -> Iterable[Tuple[pip._internal.models.link.Link, Tuple[str, str]]]
     |      Download the files given by links into location.
     |
     |  __init__(self, session: pip._internal.network.session.PipSession, progress_bar: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class Downloader(builtins.object)
     |  Downloader(session: pip._internal.network.session.PipSession, progress_bar: str) -> None
     |
     |  Methods defined here:
     |
     |  __call__(self, link: pip._internal.models.link.Link, location: str) -> Tuple[str, str]
     |      Download the file given by link into location.
     |
     |  __init__(self, session: pip._internal.network.session.PipSession, progress_bar: str) -> None
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    parse_content_disposition(content_disposition: str, default_filename: str) -> str
        Parse the "filename" value from a Content-Disposition header, and
        return the default filename if the result is empty.

    sanitize_content_filename(filename: str) -> str
        Sanitize the "filename" value from a Content-Disposition header.

DATA
    HEADERS = {'Accept-Encoding': 'identity'}
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    PyPI = <pip._internal.models.index.PackageIndex object>
    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    logger = <VerboseLogger download (WARNING)>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\network\download.py


