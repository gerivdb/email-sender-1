Help on module run_archive_tests:

NAME
    run_archive_tests - Tests complets pour le mécanisme d'archivage thématique.

FUNCTIONS
    main()
        Point d'entrée principal des tests.

    print_header(title)
        Affiche un en-tête de section.

    print_result(test_name, success)
        Affiche le résultat d'un test.

    test_archive_item(storage_path, archive_path)
        Test d'archivage d'un élément.

    test_create_instance(temp_dir)
        Test de création d'instance.

    test_get_archived_items(storage_path, archive_path)
        Test de récupération des éléments archivés.

    test_get_archived_items_by_theme(storage_path, archive_path)
        Test de récupération des éléments archivés par thème.

    test_import()
        Test d'importation des modules.

DATA
    parent_dir = r'D:\DO\WEB\N8N_tests\PROJETS\EMAIL_SENDER_1'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\src\orchestrator\run_archive_tests.py


