Help on module check_vectorization:

NAME
    check_vectorization - # -*- coding: utf-8 -*-

FUNCTIONS
    check_qdrant_collection(client, collection_name)
        Vérifier si une collection existe

    check_qdrant_connection(qdrant_url)
        Vérifier la connexion à Qdrant

    create_qdrant_collection(client, collection_name, vector_size=1536)
        Créer une collection Qdrant

    get_markdown_tasks(file_path)
        Extraire les tâches d'un fichier Markdown

    get_qdrant_collection_count(client, collection_name)
        Compter les points dans une collection

    get_qdrant_tasks(client, collection_name)
        Récupérer les tâches vectorisées dans Qdrant

    main()

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\misc\check_vectorization.py


