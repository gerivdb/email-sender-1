Help on module encoding:

NAME
    encoding

FUNCTIONS
    auto_decode(data: bytes) -> str
        Check a bytes string for a BOM to correctly detect the encoding

        Fallback to locale.getpreferredencoding(False) like open() on Python3

DATA
    BOMS = [(b'\xef\xbb\xbf', 'utf-8'), (b'\xff\xfe', 'utf-16'), (b'\xfe\x...
    ENCODING_RE = re.compile(b'coding[:=]\\s*([-\\w.]+)')
    List = typing.List
        A generic version of list.

    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    __annotations__ = {'BOMS': typing.List[typing.Tuple[bytes, str]]}

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\utils\encoding.py


