Help on module wheel_builder:

NAME
    wheel_builder - Orchestrator for building wheels from InstallRequirements.

FUNCTIONS
    build(requirements: Iterable[pip._internal.req.req_install.InstallRequirement], wheel_cache: pip._internal.cache.WheelCache, verify: bool, build_options: List[str], global_options: List[str]) -> Tuple[List[pip._internal.req.req_install.InstallRequirement], List[pip._internal.req.req_install.InstallRequirement]]
        Build wheels.

        :return: The list of InstallRequirement that succeeded to build and
            the list of InstallRequirement that failed to build.

    should_build_for_install_command(req: pip._internal.req.req_install.InstallRequirement) -> bool

    should_build_for_wheel_command(req: pip._internal.req.req_install.InstallRequirement) -> bool

DATA
    BuildResult = typing.Tuple[typing.List[pip._internal.req.req_i...ip._i...
    Iterable = typing.Iterable
        A generic version of collections.abc.Iterable.

    List = typing.List
        A generic version of list.

    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

    logger = <VerboseLogger wheel_builder (WARNING)>
    vcs = <pip._internal.vcs.versioncontrol.VcsSupport object>

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\wheel_builder.py


