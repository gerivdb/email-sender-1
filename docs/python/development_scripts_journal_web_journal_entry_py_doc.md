Help on module journal_entry:

NAME
    journal_entry

FUNCTIONS
    create_journal_entry(title, tags=None, related=None)
        Crée une nouvelle entrée dans le journal de bord.

    normalize_accents(text)
        Normalise les caractères accentués.

    update_index()
        Met à jour l'index principal du journal.

    update_tag_index(tag)
        Met à jour l'index pour un tag spécifique.

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\development\scripts\journal\web\journal_entry.py


