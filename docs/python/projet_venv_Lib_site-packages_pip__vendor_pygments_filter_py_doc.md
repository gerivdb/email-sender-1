Help on module filter:

NAME
    filter

DESCRIPTION
    pygments.filter
    ~~~~~~~~~~~~~~~

    Module that implements the default filter.

    :copyright: Copyright 2006-2024 by the Pygments team, see AUTHORS.
    :license: BSD, see LICENSE for details.

CLASSES
    builtins.object
        Filter
            FunctionFilter

    class Filter(builtins.object)
     |  Filter(**options)
     |
     |  Default filter. Subclass this class or use the `simplefilter`
     |  decorator to create own filters.
     |
     |  Methods defined here:
     |
     |  __init__(self, **options)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  filter(self, lexer, stream)
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class FunctionFilter(Filter)
     |  FunctionFilter(**options)
     |
     |  Abstract class used by `simplefilter` to create simple
     |  function filters on the fly. The `simplefilter` decorator
     |  automatically creates subclasses of this class for
     |  functions passed to it.
     |
     |  Method resolution order:
     |      FunctionFilter
     |      Filter
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, **options)
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  filter(self, lexer, stream)
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  function = None
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from Filter:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    apply_filters(stream, filters, lexer=None)
        Use this method to apply an iterable of filters to
        a stream. If lexer is given it's forwarded to the
        filter, otherwise the filter receives `None`.

    simplefilter(f)
        Decorator that converts a function into a filter::

            @simplefilter
            def lowercase(self, lexer, stream, options):
                for ttype, value in stream:
                    yield ttype, value.lower()

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\pygments\filter.py


