Help on module heuristics:

NAME
    heuristics

DESCRIPTION
    # SPDX-FileCopyrightText: 2015 Eric Larson
    #
    # SPDX-License-Identifier: Apache-2.0

CLASSES
    builtins.object
        BaseHeuristic
            ExpiresAfter
            LastModified
            OneDayCache

    class BaseHeuristic(builtins.object)
     |  Methods defined here:
     |
     |  apply(self, response: 'HTTPResponse') -> 'HTTPResponse'
     |
     |  update_headers(self, response: 'HTTPResponse') -> 'dict[str, str]'
     |      Update the response headers with any new headers.
     |
     |      NOTE: This SHOULD always include some Warning header to
     |            signify that the response was cached by the client, not
     |            by way of the provided headers.
     |
     |  warning(self, response: 'HTTPResponse') -> 'str | None'
     |      Return a valid 1xx warning header value describing the cache
     |      adjustments.
     |
     |      The response is provided too allow warnings like 113
     |      http://tools.ietf.org/html/rfc7234#section-5.5.4 where we need
     |      to explicitly say response is over 24 hours old.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors defined here:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class ExpiresAfter(BaseHeuristic)
     |  ExpiresAfter(**kw: 'Any') -> 'None'
     |
     |  Cache **all** requests for a defined time period.
     |
     |  Method resolution order:
     |      ExpiresAfter
     |      BaseHeuristic
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  __init__(self, **kw: 'Any') -> 'None'
     |      Initialize self.  See help(type(self)) for accurate signature.
     |
     |  update_headers(self, response: 'HTTPResponse') -> 'dict[str, str]'
     |      Update the response headers with any new headers.
     |
     |      NOTE: This SHOULD always include some Warning header to
     |            signify that the response was cached by the client, not
     |            by way of the provided headers.
     |
     |  warning(self, response: 'HTTPResponse') -> 'str | None'
     |      Return a valid 1xx warning header value describing the cache
     |      adjustments.
     |
     |      The response is provided too allow warnings like 113
     |      http://tools.ietf.org/html/rfc7234#section-5.5.4 where we need
     |      to explicitly say response is over 24 hours old.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from BaseHeuristic:
     |
     |  apply(self, response: 'HTTPResponse') -> 'HTTPResponse'
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from BaseHeuristic:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class LastModified(BaseHeuristic)
     |  If there is no Expires header already, fall back on Last-Modified
     |  using the heuristic from
     |  http://tools.ietf.org/html/rfc7234#section-4.2.2
     |  to calculate a reasonable value.
     |
     |  Firefox also does something like this per
     |  https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching_FAQ
     |  http://lxr.mozilla.org/mozilla-release/source/netwerk/protocol/http/nsHttpResponseHead.cpp#397
     |  Unlike mozilla we limit this to 24-hr.
     |
     |  Method resolution order:
     |      LastModified
     |      BaseHeuristic
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  update_headers(self, resp: 'HTTPResponse') -> 'dict[str, str]'
     |      Update the response headers with any new headers.
     |
     |      NOTE: This SHOULD always include some Warning header to
     |            signify that the response was cached by the client, not
     |            by way of the provided headers.
     |
     |  warning(self, resp: 'HTTPResponse') -> 'str | None'
     |      Return a valid 1xx warning header value describing the cache
     |      adjustments.
     |
     |      The response is provided too allow warnings like 113
     |      http://tools.ietf.org/html/rfc7234#section-5.5.4 where we need
     |      to explicitly say response is over 24 hours old.
     |
     |  ----------------------------------------------------------------------
     |  Data and other attributes defined here:
     |
     |  cacheable_by_default_statuses = {200, 203, 204, 206, 300, 301, ...}
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from BaseHeuristic:
     |
     |  apply(self, response: 'HTTPResponse') -> 'HTTPResponse'
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from BaseHeuristic:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

    class OneDayCache(BaseHeuristic)
     |  Cache the response by providing an expires 1 day in the
     |  future.
     |
     |  Method resolution order:
     |      OneDayCache
     |      BaseHeuristic
     |      builtins.object
     |
     |  Methods defined here:
     |
     |  update_headers(self, response: 'HTTPResponse') -> 'dict[str, str]'
     |      Update the response headers with any new headers.
     |
     |      NOTE: This SHOULD always include some Warning header to
     |            signify that the response was cached by the client, not
     |            by way of the provided headers.
     |
     |  ----------------------------------------------------------------------
     |  Methods inherited from BaseHeuristic:
     |
     |  apply(self, response: 'HTTPResponse') -> 'HTTPResponse'
     |
     |  warning(self, response: 'HTTPResponse') -> 'str | None'
     |      Return a valid 1xx warning header value describing the cache
     |      adjustments.
     |
     |      The response is provided too allow warnings like 113
     |      http://tools.ietf.org/html/rfc7234#section-5.5.4 where we need
     |      to explicitly say response is over 24 hours old.
     |
     |  ----------------------------------------------------------------------
     |  Data descriptors inherited from BaseHeuristic:
     |
     |  __dict__
     |      dictionary for instance variables
     |
     |  __weakref__
     |      list of weak references to the object

FUNCTIONS
    datetime_to_header(dt: 'datetime') -> 'str'

    expire_after(delta: 'timedelta', date: 'datetime | None' = None) -> 'datetime'

DATA
    Mapping = typing.Mapping
        A generic version of collections.abc.Mapping.

    TIME_FMT = '%a, %d %b %Y %H:%M:%S GMT'
    TYPE_CHECKING = False

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_vendor\cachecontrol\heuristics.py


