Help on module string_tools:

NAME
    string_tools - Module contenant des outils de manipulation de chaînes pour le MCP.

DESCRIPTION
    Ce module contient des outils de manipulation de chaînes qui peuvent être découverts
    et utilisés par le ToolsManager.

FUNCTIONS
    count_char(text: str, char: str) -> int
        Compte le nombre d'occurrences d'un caractère dans une chaîne.

        Args:
            text (str): Chaîne à analyser
            char (str): Caractère à compter

        Returns:
            int: Nombre d'occurrences

    reverse_string(text: str) -> str
        Inverse une chaîne.

        Args:
            text (str): Chaîne à inverser

        Returns:
            str: Chaîne inversée

    to_lower(text: str) -> str
        Convertit une chaîne en minuscules.

        Args:
            text (str): Chaîne à convertir

        Returns:
            str: Chaîne convertie en minuscules

    to_upper(text: str) -> str
        Convertit une chaîne en majuscules.

        Args:
            text (str): Chaîne à convertir

        Returns:
            str: Chaîne convertie en majuscules

DATA
    parent_dir = r'D:\DO\WEB\N8N_tests\PROJETS\EMAIL_SENDER_1'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\src\mcp\examples\tools\string_tools.py


