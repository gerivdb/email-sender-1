Help on module test_version_control:

NAME
    test_version_control - Test pour les fonctionnalités de gestion des versions.

FUNCTIONS
    create_test_data(storage_path)
        Crée des données de test.

    main()
        Point d'entrée principal des tests.

    print_header(title)
        Affiche un en-tête de section.

    print_result(test_name, success)
        Affiche le résultat d'un test.

    test_compare_versions(storage_path, versions_path, items)
        Teste la comparaison de versions.

    test_create_version(storage_path, versions_path, items)
        Teste la création de versions.

    test_get_version(storage_path, versions_path, items)
        Teste la récupération d'une version spécifique.

    test_get_versions(storage_path, versions_path, items)
        Teste la récupération des versions.

    test_manager_version_control(storage_path, versions_path, archive_path, items)
        Teste les fonctionnalités de gestion des versions du gestionnaire CRUD.

    test_restore_version(storage_path, versions_path, items)
        Teste la restauration d'une version.

DATA
    parent_dir = r'D:\DO\WEB\N8N_tests\PROJETS\EMAIL_SENDER_1'

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\src\orchestrator\test_version_control.py


