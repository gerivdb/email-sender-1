Help on module glibc:

NAME
    glibc

FUNCTIONS
    glibc_version_string() -> Optional[str]
        Returns glibc version string, or None if not using glibc.

    glibc_version_string_confstr() -> Optional[str]
        Primary implementation of glibc_version_string using os.confstr.

    glibc_version_string_ctypes() -> Optional[str]
        Fallback implementation of glibc_version_string using ctypes.

    libc_ver() -> Tuple[str, str]
        Try to determine the glibc version

        Returns a tuple of strings (lib, version) which default to empty strings
        in case the lookup fails.

DATA
    Optional = typing.Optional
        Optional[X] is equivalent to Union[X, None].

    Tuple = typing.Tuple
        Deprecated alias to builtins.tuple.

        Tuple[X, Y] is the cross-product type of X and Y.

        Example: Tuple[T1, T2] is a tuple of two elements corresponding
        to type variables T1 and T2.  Tuple[int, float, str] is a tuple
        of an int, a float and a string.

        To specify a variable-length tuple of homogeneous type, use Tuple[T, ...].

FILE
    d:\do\web\n8n_tests\projets\email_sender_1\projet\venv\lib\site-packages\pip\_internal\utils\glibc.py


