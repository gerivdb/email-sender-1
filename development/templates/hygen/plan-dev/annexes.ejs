<% /*
  Générateur d'annexes pour Hygen
  Utilisation :
  - Inclure ce fichier dans plan-dev.ejs avec la syntaxe EJS :
    <%~ include('annexes.ejs', { annexes: annexes }) %>
  - 'annexes' doit être un tableau de chaînes de caractères
*/ %>
<% if (typeof annexes !== 'undefined' && annexes.length > 0) { %>
## Annexes
<% annexes.forEach(function(a) { %>
- <%= a %>
<% }) %>
<% } %>
