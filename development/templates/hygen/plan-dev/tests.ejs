<% /*
  Générateur de scénarios de test avec catégories
  Utilisation :
  - Inclure ce fichier dans plan-dev.ejs avec la syntaxe EJS :
    <%~ include('tests.ejs', { tests: tests }) %>
  - 'tests' doit être un tableau d'objets { category: 'Catégorie', description: 'Description' }
*/ %>
<% if (typeof tests !== 'undefined' && tests.length > 0) { %>
## Scénarios de test
<% tests.forEach(function(test, idx) { %>
- **Test <%= idx+1 %>** (<%= test.category %>) : <%= test.description %>
<% }) %>
<% } %>
